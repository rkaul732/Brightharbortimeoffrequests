{
  "name": "bright-harbor-time-off-requests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check public/app.js && node --check netlify/functions/*.mjs && node --check netlify/functions/_shared/*.mjs",
    "serve": "python3 -m http.server 8080 --directory public"
  },
  "engines": {
    "node": ">=20"
  }
}
